# feregion verification traceability

| Field | Value |
|---|---|
| Status | Current beta traceability |
| Current target | `0.4.0` benchmark-system beta stabilization; ASV migration closed and primary benchmark authority established |

The ASV-derived path is the primary benchmark authority; the 0.4 migration is closed.

## Purpose

This document identifies how each current requirement is assessed. It separates the intended assessment method from the observed release result. The delivery-side verification record is the authority for commands that actually ran, their environment, skips, failures, and limitations. A workflow definition or test path is not itself a passing verification result.

## Requirement assessment map

| Requirement | Primary assessment method | Required environment | Current-result rule | Known limitation |
|---|---|---|---|---|
| `REQ-API-001` | tests/test_public_api.py, tests/test_region.py, tests/test_engine_data_invariants.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-API-002` | tests/test_public_api.py, tests/test_region.py, tests/test_engine_data_invariants.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-API-003` | tests/test_public_api.py, tests/test_region.py, tests/test_engine_data_invariants.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-API-004` | tests/test_seismic.py plus public API tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Active geographical IDs resolve names; retired/unused geographical IDs raise `RegionNumberError`. | Historical name slots may remain stored without becoming active. |
| `REQ-API-005` | tests/test_public_api.py, tests/test_region.py, tests/test_engine_data_invariants.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-API-006` | tests/test_public_api.py, tests/test_region.py, tests/test_engine_data_invariants.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-001` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-002` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-003` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-004` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-005` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-006` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-007` | tests/test_cli.py transactional filesystem-failure tests and installed CLI smoke verification | Runtime suite plus installed-artifact verification | File-output failures preserve the destination and the diagnostic states that no partial destination was published. | Crash durability and filesystem metadata beyond the declared contract remain outside this check. |
| `REQ-CLI-008` | tests/test_cli.py late-failure stdout test and installed CLI smoke verification | Runtime suite plus installed-artifact verification | Earlier streamed rows may remain visible and the failure diagnostic tells the operator to discard partial stdout before retry. | Caller-owned shell redirection and downstream pipeline behavior remain outside the package contract. |
| `REQ-CLI-009` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-010` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-011` | tests/test_cli.py and installed CLI smoke verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-CLI-012` | tests/test_cli.py invalid-UTF-8 and malformed-CSV regressions | Python 3.13 local plus delivery evidence; hosted CI where configured | Local regressions pass; the invalid-UTF-8 case has preserved predecessor sensitivity evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-001` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-002` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-003` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-004` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-005` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-006` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-007` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-008` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-009` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-010` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DATA-011` | tests/test_packaged_assets.py, fetch/source-reproduction tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DEL-001` | delivery-side archive/patch/manifest/checksum/replay checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DEL-002` | delivery-side archive/patch/manifest/checksum/replay checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DEL-003` | delivery-side archive/patch/manifest/checksum/replay checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DEL-004` | delivery-side archive/patch/manifest/checksum/replay checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-DEL-005` | source-archive inventory, incremental patch inventory, and delivery manifest | iterative delivery construction | `uv.lock` is absent from source and patch; supplied lock hunks are outside delivery reconstruction. | The maintainer must regenerate the lock locally before locked repository workflows run. |
| `REQ-ERR-001` | core invalid-input tests, pandas tests, exact-exception helper | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-ERR-002` | core invalid-input tests, pandas tests, exact-exception helper | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-ERR-003` | core invalid-input tests, pandas tests, exact-exception helper | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-ERR-004` | core invalid-input tests, pandas tests, exact-exception helper | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-ERR-005` | core invalid-input tests, pandas tests, exact-exception helper | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-ERR-006` | core invalid-input tests, pandas tests, exact-exception helper | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-FE-001` | core scalar/batch tests plus source-reproduction differential tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-FE-002` | core scalar/batch tests plus source-reproduction differential tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-FE-003` | core scalar/batch tests plus source-reproduction differential tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-FE-004` | core scalar/batch tests plus source-reproduction differential tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-GEO-001` | tests/test_geojson.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-GEO-002` | tests/test_geojson.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-GEO-003` | tests/test_geojson.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-GEO-004` | tests/test_geojson.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-GEO-005` | tests/test_geojson.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-NP-001` | core array positive/invalid/boundary tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-NP-002` | core array positive/invalid/boundary tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-NP-003` | core array positive/invalid/boundary tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-NP-004` | tests/test_names.py scalar/array conversion tests plus invalid-number tests | Runtime suite and downstream typing fixture | Name conversion preserves input shape and always returns a Unicode ndarray, including a zero-dimensional array for scalar `ArrayLike` input. | Scalar-specific name APIs intentionally return Python strings. |
| `REQ-NP-005` | core array positive/invalid/boundary tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-NP-006` | core array positive/invalid/boundary tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-NP-007` | core array positive/invalid/boundary tests | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-001` | tests/test_pandas.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-002` | tests/test_pandas.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-003` | tests/test_pandas.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-004` | tests/test_pandas.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-005` | tests/test_pandas.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-006` | tests/test_pandas.py | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-007` | tests/test_pandas.py::test_dataframe_wide_finite_out_of_range_preserves_core_error_precedence | Platform where longdouble exponent range exceeds float64 | Local regression passes and has preserved predecessor sensitivity evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PD-008` | tests/test_pandas.py nullable-missing and legacy-object regressions plus lower-bound tox/CI environment | Supported pandas nullable numeric dtypes, including declared lower bound | Missing nullable numeric coordinates reach core non-finite validation and raise `CoordinateValueError`. | Current local pandas alone does not establish lower-bound compatibility. |
| `REQ-PERF-001` | `benchmarks/asv_suite/` plus `tests/test_asv_benchmark_contract.py` | Repository tests plus retained ASV smoke evidence | Every required in-process case is represented under one stable semantic identity and the dedicated ASV suite is isolated from predecessor benchmark modules. | Real b1 smoke execution is observed; corrected-reference rerun remains separate evidence. |
| `REQ-PERF-002` | `benchmarks/evidence.py`, campaign-plan tests, and retained ASV evidence when produced | Controlled benchmark host plus retained raw samples | Evidence contains case/workload/revision/environment/machine/tool identity, resolved campaign, correctness state, aggregate definition, and sample linkage. | Accepted migration evidence retains raw samples plus result/state/run/environment/report provenance; ASV is now the primary benchmark path. |
| `REQ-PERF-003` | project metadata tests, source-distribution inspection, benchmark dependency group, and `.gitignore` | Repository/package verification | ASV/project benchmark tooling is repository-side, present in source deliveries, absent from runtime wheel, and generated result/history/HTML artifacts remain outside source. | Source layout is implemented; generated real-ASV artifacts remain operational evidence. |
| `REQ-PERF-004` | predecessor comparator plus `benchmarks/asv_suite/benchmarks.py` scalar setup/invariant checks | Benchmark environment with pinned source tables; ObsPy where requested | Scalar case correctness agrees with the declared comparator/invariant before timing; performance remains diagnostic. | Corrected b2 reference evidence passes environment integrity and retains finite feregion, ObsPy, and pinned-source scalar measurements. |
| `REQ-PERF-005` | `benchmarks.campaign.PROFILE_CONFIG`, environment preflight, campaign fixtures, and profile-resolution tests | Hosts with selected Python/dependency versions available | Resolved requested environments match reviewed profile combinations and b2 preflight confirms installed versions/dependency consistency before timing. | Corrected b2 reference evidence records requested=observed dependencies, successful imports, and a clean dependency check. |
| `REQ-PERF-006` | predecessor `compare_releases.py`; `benchmarks/regression.py`; threshold-boundary and campaign-check tests | Comparable baseline/candidate throughput records on one context | Both paths compute slowdown in throughput space; +26% duration does not falsely cross a >25% throughput rule; >33.33% duration can cross; missing/incorrectness-unverified evidence remains incomplete. | Post-b3 normalized a9↔b3 evidence contains all seven required same-profile loads; the gate is complete and does not trigger. |
| `REQ-PERF-007` | predecessor benchmark commands plus ASV configuration/integration tests and parity review | Benchmark development environment with predecessor and ASV dependencies | Predecessor remains authoritative during migration; ASV performs target generic roles without silently deleting predecessor evidence roles. | The migration-parity requirement is satisfied; `DEC-062` makes the ASV-derived path primary and retains predecessor tooling only for compatibility/reference/provenance. |
| `REQ-PERF-008` | contracts/workloads/source-oracle/evidence adapter plus semantic-version fixtures | Current/historical ASV results and pinned source tables | Stored ASV benchmark version must map to the project case version; unknown versions normalize as incompatible; wrong-but-in-range outputs fail source-oracle checking. | Current/history/reference/diagnostic execution is retained; corrected b2 direct ObsPy reference passes the verified environment contract. |
| `REQ-PERF-009` | historical adapters, `.asv/feregion-state` setup markers, `.asv/feregion-runs` revision records, `.asv/feregion-environments` preflight records, evidence fixtures | ASV historical revision environments | Not-applicable, environment/oracle unavailable, correctness failed, execution failed, incompatible, and measured states remain distinguishable. | Integration fixtures distinguish rare failure states; retained evidence supplies genuine historical not-applicable and real environment-integrity failure examples. Deliberate destructive induction is not required. |
| `REQ-PERF-010` | campaign schema/parser, content-addressed plan sidecars, and deterministic plan/run-link tests | Repository test environment | Campaign resolves purpose, revisions, cases, load sizes, effective timing controls, environment profile, sample/append policy, machine/comparability policy, report steps, source-config identity, and tool identity; each run retains the exact plan. | Implemented with `.asv/feregion-plans/`; revision-run records link the plan digest and tests cover non-default controls. |
| `REQ-PERF-011` | Target: campaign CLI delegation tests plus retained ASV smoke | Repository test environment; ASV for integration smoke | Planning/running/comparing/report-building responsibilities delegate generic execution to ASV and do not contain an independent timer/environment/result/site implementation. | Delegation is repository-tested and real b1 smoke execution is observed. |
| `REQ-PERF-012` | pinned-source oracle module, ASV setup bindings, deliberately wrong in-range sensitivity test, retained reference/smoke evidence | Hash-verified pinned FE source cache | Bounded deterministic source-oracle checking runs before timing; seeded wrong identities fail; source/oracle work is excluded from timed methods. | Real b1 source-reference measurements and correctness-passed sidecars are observed; corrected environment verification is required for future accepted timings. |
| `REQ-PERF-013` | Target: authoritative ASV run fixture with sample recording plus evidence-normalization tests | Controlled benchmark host | Authoritative result retains raw samples and normalized aggregates are traceable to them. | Accepted ASV evidence retains raw samples and normalized aggregates; corrected reference and release-comparison evidence are included in the migration checkpoint. |
| `REQ-PERF-014` | Target: campaign-plan/command tests and run documentation review | Benchmark host | Timed measurement is serial by default; future parallel timing requires a separate decision and evidence. | Serial campaign execution is implemented and real b1 smoke/history/reference/diagnostic/dependency/Python runs are observed; b2 does not introduce parallel timing. |
| `REQ-PERF-015` | Target: ASV static-site rebuild/publication verification | Durable benchmark-history store plus static-site environment | Retained results rebuild the site without new measurements; source excludes result/history/HTML artifacts; publication state is verified separately when pushed. | Post-b3 retained report evidence proves local static-site rebuild from preserved evidence; external publication remains a separate unverified state unless explicitly checked. |
| `REQ-PERF-016` | `benchmarks/evidence.py` schema-v2 fixtures, retained run/environment tool identity, and version/state/sample tests | Representative ASV v2 results plus state/run/environment sidecars | Evidence preserves stored benchmark version, mapped case version, result/correctness state, operation count, operations-per-second, samples/statistic, machine/environment/campaign, exact operator ASV identity and observed benchmark-environment `asv-runner` identity when available, and source-result locator. | New campaign runs retain operator ASV identity in run evidence and the actual benchmark-environment `asv-runner` in environment evidence; normalization prefers the observed runner identity and older records may remain null when the older sidecars did not record it. |
| `REQ-PERF-017` | `docs/benchmark-migration-parity.md`, predecessor harnesses, ASV comparator/diagnostic campaigns, real vertical-slice parity report | One controlled host capable of both paths | Predecessor remains authoritative until semantic outputs, case/metric parity, throughput gate, applicability/failure states, raw samples and report rebuild are reviewed as equivalent or explicitly superseded. | **Satisfied.** Reviewed post-b3 evidence provides same-profile release-gate parity, corrected reference/diagnostic coverage, raw samples, state discrimination, fresh predecessor evidence, and successful retained report rebuild; `DEC-062` changes benchmark authority accordingly. |
| `REQ-PERF-018` | `tests/test_asv_benchmark_contract.py`, dedicated `benchmarks/asv_suite/`, direct ASV-parser oracle, and CI benchmark-contract job | Repository test environment; ASV installed for direct parser/smoke evidence | Generated argv uses subcommand-local `--config`; config location preserves repository-relative ASV paths; temporary config is removed; ASV discovery excludes predecessor pytest tooling; partial load filters match only selected values. | CI installs the benchmark group and runs the focused contract module, so the direct ASV parser test must execute non-skipped there; `asv check` remains an additional tool-boundary check. |
| `REQ-PERF-019` | `benchmarks/campaign.py`, `asv.conf.json`, and real-Git/build-contract tests in `tests/test_asv_benchmark_contract.py` | Repository test environment with Git; ASV 0.6.6 host for final integration smoke | Each requested identity resolves to one immutable SHA; `asv run` receives `<sha>^!`; missing/range-like inputs fail before ASV work; effective ASV config builds one project wheel with `--no-deps` and installs without dependency resolution. | Real b1 campaigns exercise Git/config/build-install boundaries and regression tests cover exact selectors; the corrected b2 reference environment still requires a host rerun. |
| `REQ-PERF-020` | `benchmarks/asv_suite/contracts.py`, ASV batch parameterization, regression adjacency tests | Repository tests; high-memory benchmark host for upper sizes | Standard grid is exactly the 1-2-5 sequence through 50,000,000; parameter filtering accepts each size exactly; release adjacency uses maintained neighboring sizes rather than surviving-result order. | Source contract is deterministic; successful 10M–50M execution remains host/resource-dependent measurement evidence. |
| `REQ-PERF-021` | `benchmarks/campaigns/*.toml`, explicit-baseline campaign tests, and `docs/benchmark-operations.md` | Repository review plus operator benchmark host | Canonical campaigns cover smoke, full HEAD, explicit-baseline release comparison, history, Python/NumPy/pandas sensitivity and sparse dependency matrix; the release baseline is supplied at execution, resolved immutably, and retained in the effective plan. | b6 removes the mutable baseline source and verifies fail-closed explicit baseline binding; external publication remains separately verified state. |
| `REQ-PERF-022` | `benchmarks/evidence.py`, `benchmarks/regression.py`, `benchmarks/campaign.py`, ASV-result fixture tests | Retained ASV v2 results from one comparable host/environment | `campaign check` reconstructs normalized evidence without timing work, requires complete selected >=10k load evidence, distinguishes ambiguous/incomplete evidence, and returns pass/trigger/incomplete status. | Synthetic/fixture coverage verifies parsing and decision logic; authoritative result depends on real retained measurements. |
| `REQ-PERF-023` | `benchmarks/asv_suite/benchmarks.py`, `benchmarks/asv_plugin.py`, `benchmarks/reporting.py`, `benchmarks/report_assets/`, and reporting contract tests | Repository tests plus ASV 0.6.x static-site publication on an operator host | Every maintained benchmark exposes human-readable display/source metadata; retained ASV publication adds the project summary, coverage/regression context, and curated scaling links without replacing native ASV result/history/regression machinery. | Additive insertion is version-bounded to the reviewed ASV 0.6.x template; a real a9 publication remains host integration evidence. |
| `REQ-PERF-024` | `benchmarks/release_workflow.py`, maintained campaign files, proportional-scope tests, and `docs/benchmark-operations.md` | Repository tests plus operator benchmark host | Default refresh runs the bounded explicit-baseline release gate; integration/promotion/history/diagnostic scopes can add the broader maintained portfolio, timing/sample controls, project check, and report rebuild; preview and external push remain separate. | b6 verifies both bounded default behavior and explicit promotion/history/diagnostic expansion; append mode can still yield unequal sample counts that remain visible in raw evidence. |
| `REQ-PERF-025` | `docs/benchmark-results.md`, `docs/obspy-or-feregion.md`, `docs/benchmark-roadmap.md`, README/runbook links, and documentation synchronization tests | Documentation review against retained populated ASV evidence supplied by the maintainer | Evidence summaries state machine/environment basis and limitations; user guidance distinguishes capability evidence from unmeasured direct ObsPy speed claims; planned enhancements remain explicitly non-authoritative. | Maintained docs are reconciled through the accepted post-b3 evidence checkpoint; observed ratios remain single-host evidence and require refresh after material new measurements. |
| `REQ-PERF-026` | `benchmarks/evidence.py` plus ASV-v2 sample-shape regression fixtures in `tests/test_asv_benchmark_contract.py` | Repository test environment with representative retained-result structures | A parameterized result whose earlier sample entry is null still selects the requested parameter's sample list; unrelated parameter samples are not flattened; unsupported deeper nesting fails descriptively. | Fixture coverage verifies the supported result contract; materially different future ASV result formats require adapter review. |
| `REQ-PERF-027` | `docs/benchmark-migration-parity.md`, predecessor benchmark suite, ASV reference/diagnostic campaigns, normalized evidence tests | Controlled predecessor + ASV benchmark environment | Every predecessor case/metric role is mapped; direct ObsPy/source and diagnostics are retained; normalized ASV records expose operations-per-second; no predecessor retirement occurs before reviewed replacement evidence. | Accepted parity review retains direct ObsPy/source, diagnostic, supported-Python, predecessor-reference, operations-per-second, and corrected same-profile release evidence. |
| `REQ-PERF-028` | `benchmarks/evidence_bundle.py` and `tests/test_benchmark_environment.py` | Repository checkout with any subset of retained benchmark evidence | One command emits a ZIP with manifest, hashes, present/missing family status, ASV results/state/run/plan/environment/report evidence, predecessor outputs and stable configuration; derived HTML is excluded. | Explicit release-baseline intent is preserved inside content-addressed effective plans rather than a mutable source file. |
| `REQ-PERF-029` | `benchmarks/asv_suite/environment.py`, ASV setup bindings, reference-profile regression tests, retained `.asv/feregion-environments` records | ASV-managed benchmark environment with `asv-env-info.json` and pip | Requested and observed dependency versions agree; requested imports succeed; `pip check` succeeds; integrity failure becomes environment/build unavailability instead of accepted timing or required-dependency `not_applicable`. | Verified by the corrected b2 reference run: requested and observed versions agree, ObsPy imports, `pip check` passes, and finite comparator timings are retained. |
| `REQ-PERF-030` | `benchmarks.campaign.expected_environment_names`, `benchmarks.evidence.collect_asv_evidence`, release-check regression tests | Retained ASV results containing same case/revision across multiple environment profiles | Normalized campaign evidence accepts only environments permitted by the campaign profile; cross-profile rows are excluded and missing required-profile candidate evidence remains incomplete. | Implemented in b3 after post-b2 release evidence exposed reference-profile contamination of the release check. |
| `REQ-PERF-031` | `benchmarks.release_workflow`, `.asv/feregion-reports`, `benchmarks.evidence_bundle`, report-record tests | Retained ASV evidence plus local `asv publish` execution | Report rebuild records command outcome, source-evidence content digest, and generated HTML presence/count; handoff preserves the record while excluding derived HTML. | Verified post-b3: retained rebuild record has return code 0, HTML index present, 127 HTML files, and a content digest for the 1,020-file source-evidence set. |
| `REQ-PKG-001` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-002` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-003` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-004` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-005` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-006` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-007` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-008` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-009` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-010` | tests/test_project_metadata.py, CI workflow, distribution build/wheel verification | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-011` | .github/workflows/ci.yml reference-oracle job | Hosted CI with ObsPy dependency installed | Configured, not observed in this local iteration. | Requires hosted CI execution on exact target source. |
| `REQ-PKG-012` | .github/workflows/ci.yml minimum-dependencies job | Hosted Python 3.11 with declared dependency lower bounds | Configured, not observed locally. | Requires package-index access. |
| `REQ-PKG-013` | tools/verify_wheel.py archive inspection plus clean uv installation | Built wheel and dependency-resolving clean environment | Archive inspection can run locally; dependency-isolated install depends on package-index access. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-014` | docs/feregion-quality-assurance.md | Python 3.13 local plus delivery evidence; hosted CI where configured | Current quality scenarios and release gates are defined. | Method coverage is not a claim outside the recorded environment. |
| `REQ-PKG-015` | quality-assurance release-validation gate plus hosted CI and uv.lock state | Python 3.13 local plus delivery evidence; hosted CI where configured | A beta candidate is not called release-validated or promotion-gate complete until the required evidence is observed. | A beta source handoff may remain partially verified if limitations are explicit. |
| `REQ-PKG-016` | pyproject.toml, .pre-commit-config.yaml, tox.toml, tests/test_project_metadata.py | uv development environment plus pre-commit | `pre-commit` is a development dependency; hooks run Ruff format/check and delegate behavioral tests to tox `local` through a lock-preserving `uv --no-sync` path. | Hook execution still depends on the development environment being synchronized. |
| `REQ-PKG-017` | tests/test_project_metadata.py plus `.github/workflows/ci.yml` and `pyproject.toml` | Hosted CI and repository metadata checks | uv compatibility-range and lock-preserving normal jobs match the declared contract. | Minimum-dependency job intentionally bypasses the project lock. |
| `REQ-PKG-018` | tests/test_project_metadata.py plus `.github/workflows/ci.yml` | Hosted CI workflow behavior | Job timeouts and workflow concurrency are configured; push scope is the default branch and pull requests remain verified. | Configuration is not evidence that a hosted run completed. |
| `REQ-PKG-019` | `tox.toml`, tests/test_project_metadata.py, and `.github/workflows/ci.yml` | Lock-backed local tox-uv matrix plus hosted lower-bound job | `py311` through `py314` share the locked test definition; `minimum` co-resolves the project plus test extra under `lowest-direct` and recreates before each run; hosted completion is recorded separately. | Matrix configuration does not itself prove every interpreter/dependency environment passed. |
| `REQ-REPO-001` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-002` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-003` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-004` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-005` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-006` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-007` | repository metadata checks plus source-archive inventory/replay | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-008` | docs/feregion-decisions.md | Python 3.13 local plus delivery evidence; hosted CI where configured | Current consequential decisions and review triggers are recorded. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-009` | this traceability document plus delivery verification record | Python 3.13 local plus delivery evidence; hosted CI where configured | Assessment method and release result are separated. | Method coverage is not a claim outside the recorded environment. |
| `REQ-REPO-010` | `tools/export_repository.py` and `tests/test_export_repository.py` | Temporary Git repositories plus delivery-side handoff use | Export tests verify tracked working-tree bytes, deterministic ZIP output for fixed destinations, executable state, `uv.lock` exclusion, untracked-file handling, and the default `dist/feregion-v<version>-<UTC-date>-handoff.zip` identity. | New untracked source must be staged/committed or strict export will reject the handoff; an explicit `--output` may select another destination. |
| `REQ-TEST-001` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-002` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-003` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-004` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-005` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-006` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-007` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-008` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-009` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-010` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-011` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-012` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-013` | pytest suite and repository metadata checks | Python 3.13 local plus delivery evidence; hosted CI where configured | Release-specific result is recorded in delivery or CI evidence. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-014` | review-resolution sensitivity record plus named regression execution | Python 3.13 local plus delivery evidence; hosted CI where configured | Satisfied for predecessor boundary defects and the pandas nullable-object compatibility correction. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-015` | tests/test_packaged_assets.py::test_packaged_asset_hashes_match_recorded_metadata | Python 3.13 local plus delivery evidence; hosted CI where configured | Local test result recorded in release verification. | Method coverage is not a claim outside the recorded environment. |
| `REQ-TEST-016` | `tox.toml`, tests/test_project_metadata.py, and lower-bound CI job | CPython 3.11–3.14 plus Python 3.11 lowest-direct environment | Local matrix definition and hosted lower-bound definition share the same named tox environment. | Missing local interpreters/network can prevent local execution and must be recorded. |

| `REQ-HIER-001` | tests/test_seismic.py plus public API/documentation checks | Runtime suite and documentation review | Explicit level-specific API and geographical compatibility aliases must agree. | Historical generic terminology remains supported for compatibility. |
| `REQ-HIER-002` | tests/test_packaged_assets.py and ISC normalized-source tests | Packaged assets plus literal semantic-pin tests | Exactly 50 seismic and 754 active geographical identifiers must be represented. | Scientific source authority is recorded separately from software implementation. |
| `REQ-HIER-003` | tests/test_packaged_assets.py, tests/test_seismic.py | Runtime suite | Every active geographical ID has one nonzero parent; retired IDs remain inactive for name/hierarchy use and zero in packaged crosswalk storage. | Does not independently resolve data licensing. |
| `REQ-HIER-004` | tests/test_seismic.py exhaustive global-cell invariant | Runtime suite | Coordinate seismic lookup equals geographical lookup followed by crosswalk for every one-degree cell center. | Exact boundary-point behavior remains governed by geographical lookup semantics. |
| `REQ-HIER-005` | tests/test_packaged_assets.py and metadata/notice review | Runtime suite plus documentation review | All 50 packaged seismic names are non-empty and source-specific provenance remains explicit. | Source spelling is preserved even when it appears anomalous. |
| `REQ-API-007` | tests/test_seismic.py, tests/test_public_api.py | Runtime suite | Explicit geographical/seismic functions and immutable value types work; compatibility aliases remain geographical. | Does not promise removal of generic names. |
| `REQ-API-008` | tests/test_seismic.py | Runtime suite | Two-array explicit engines remain geographical-only; active geographical IDs are derived from table membership; unused name/crosswalk slots remain inactive. | Custom hierarchy compatibility is not inferred. |
| `REQ-NP-008` | tests/test_seismic.py plus batch validation suite | Runtime suite | Seismic batch result preserves `(n,)` shape and `uint8` semantics. | Performance is assessed separately. |
| `REQ-NP-009` | tests/test_seismic.py scalar/array hierarchy conversion tests plus typing fixture | Runtime suite and downstream typing fixture | Hierarchy conversion preserves input shape and always returns a `uint8` ndarray, including a zero-dimensional array for scalar `ArrayLike` input. | Retired identifiers remain invalid inputs. |
| `REQ-PD-009` | tests/test_pandas.py | Runtime suite with pandas | Both levels use documented defaults; omitted level preserves geographical behavior. | pandas remains optional. |
| `REQ-ERR-007` | tests/test_seismic.py and tests/test_geojson.py | Runtime suite | Missing seismic capability and invalid GeoJSON level/options use package-specific exceptions. | Error-message wording is not a stable API unless separately documented. |
| `REQ-CLI-013` | tests/test_cli.py plus installed-wheel CLI smoke | Runtime suite and package verification | Point/CSV/GeoJSON level selection works and default remains geographical. | GeoJSON command requires optional geometry dependency. |
| `REQ-CLI-014` | tests/test_cli.py GeoJSON multi-property, `all`, and compatibility-option tests | Runtime suite with Shapely | Multi-property selection and level-aware `all` expansion are verified; repeatable `--property` remains accepted. | Shell parsing beyond argparse behavior is outside the package contract. |
| `REQ-GEO-006` | tests/test_geojson.py | Runtime suite with Shapely | Controlled level-specific vocabulary is verified; seismic child membership uses `geographic_regions` number/name objects and rejects legacy parallel child lists. | Arbitrary templates are intentionally out of scope. |
| `REQ-GEO-007` | tests/test_geojson.py | Runtime suite with Shapely | Optional number/name/combined labels are generated from current-level values. | Renderer-specific styling remains outside the package. |
| `REQ-GEO-008` | tests/test_geojson.py | Runtime suite with Shapely | Collection metadata is emitted once by default and can be omitted. | GeoJSON consumers may ignore foreign members. |
| `REQ-GEO-009` | tests/test_geojson.py | Runtime suite with Shapely | Default feature properties are current-level `number` and `name`. | Generic property names are intentionally level-relative. |
| `REQ-GEO-010` | tests/test_geojson.py | Runtime suite with Shapely | Explicit-engine geometry/properties follow that engine; populated inactive crosswalk slots remain excluded. | Explicit-engine provenance remains separately constrained by the metadata contract. |
| `REQ-GEO-011` | tests/test_geojson.py plus core active-ID tests | Runtime suite with Shapely | GeoJSON child enumeration uses engine-owned active membership and aligns names with numbers. | Helper remains package-internal. |
| `REQ-DATA-012` | tests/test_packaged_assets.py plus metadata/THIRD_PARTY_NOTICES review | Runtime suite and documentation review | Metadata schema 3 separates ObsPy, Young-1996, and ISC roles and unresolved licenses. | This is provenance evidence, not a legal determination. |
| `REQ-DATA-013` | tests/test_packaged_assets.py, wheel inspection/install smoke, source tools | Runtime/package verification | All four processed assets ship and normal runtime declares no network/source-retrieval requirement. | Source regeneration itself requires retrieved upstream inputs. |
| `REQ-DATA-014` | tests/test_fetch_isc_fe_regions.py | Deterministic source-tool tests; optional live integration | Parser validates 50/754 coverage and normalized semantic SHA before atomic publication. | Current local environment may not permit live ISC retrieval; record that separately. |
| `REQ-DATA-015` | tests/test_packaged_assets.py, tests/test_resources_failures.py | Runtime suite | Crosswalk/name shapes, dtypes, sentinels, active coverage, and read-only loading are verified. | Asset byte identity is version-specific. |
| `REQ-TEST-017` | tests/test_seismic.py and tests/test_packaged_assets.py | Runtime suite | Exhaustive cell hierarchy invariant and complete identifier coverage execute successfully. | Exhaustive test covers the one-degree cell model, not every real-valued coordinate. |
| `REQ-TEST-018` | tests/test_fetch_isc_fe_regions.py | Runtime suite without live network | Parsing, semantic hashing, validation, and normalized publication are exercised deterministically. | Live page compatibility remains a separate integration observation. |

| `REQ-FE-005` | README, core docstrings, GeoJSON metadata, documentation review | Runtime/docs review | WGS84 is identified as a package input convention and CRS transformation is explicitly absent. | Does not claim historical FE datum authority. |
| `REQ-NP-010` | tests/test_extended_precision_boundaries.py plus core batch tests | Platforms with wider `longdouble`; ordinary batch suite elsewhere | Scalar and batch geographical/seismic results agree at FE discontinuities without pre-index narrowing. | Wider-than-float64 coverage is platform-conditional. |
| `REQ-PD-010` | tests/test_extended_precision_boundaries.py::test_pandas_preserved_longdouble_matches_scalar_at_degree_boundary | pandas plus platform preserving wider dtype | pandas routes preserve corrected scalar/batch cell ownership. | Test skips when pandas/platform cannot preserve wider dtype. |
| `REQ-DATA-016` | tools/isc_fe_source.py and tests/test_fetch_isc_fe_regions.py | Repository source-tool tests | Expected hierarchy matches a literal reviewed semantic SHA; in-memory name/membership changes do not update that identity. | Live source agreement is separate integration evidence; source licensing remains unresolved. |
| `REQ-TEST-019` | tests/test_extended_precision_boundaries.py plus predecessor sensitivity record | Wider-longdouble platform and delivery evidence | Boundary corpus covers every interior integer longitude/latitude boundary plus antimeridian, zero, and poles; predecessor execution demonstrates failure. | Diagnostic corpus is not a prevalence estimate. |
| `REQ-TEST-020` | tests/test_public_api.py | Runtime suite with optional pandas/Shapely where available | All top-level convenience routes, module entry point, adapters, GeoJSON routes, and geographic-only seismic failures are directly routed. | Deep behavior remains covered by engine tests. |
| `REQ-TEST-021` | tests/test_geojson.py all-cell-center coverage test | Runtime suite with Shapely | Every global one-degree cell center is covered by the numeric-owner feature at both levels. | Exact polygon-edge equivalence remains intentionally unclaimed. |
| `REQ-TEST-022` | tests/test_exhaustive_grid_boundaries.py | Runtime suite; wider-longdouble cases are platform-conditional | All 64,800 area cells and all integer grid-vertex neighborhoods preserve independently constructed quadrant/latitude/longitude index ownership for supported floating dtypes. | This verifies coordinate-to-dense-index semantics; source-table and hierarchy tests separately verify scientific table content. |
| `REQ-PKG-020` | pyproject mypy config, .pre-commit-config.yaml, tests/typing/public_api.py, quality CI job | Local pre-commit and hosted quality environments | Ruff remains lint/format evidence; mypy checks public modules and downstream built-in/NumPy scalar use. | Local execution may be unavailable without resolved dev dependencies. |
| `REQ-PKG-021` | .github/workflows/ci.yml and tests/test_project_metadata.py | Scheduled/manual hosted CI | Live ISC fetch/semantic verification is isolated from ordinary PR tests. | A configured job is not evidence that a hosted run occurred. |

## Release evidence record

For each delivered version, the external verification record must state the exact source identity, command or assessment, environment, observed result, skipped or unavailable scope, and known limitation. The quality-assurance document defines the release gates that consume these records.

## Traceability acceptance

This target contains **170 unique requirement IDs**. Repository synchronization tests verify that every current requirement ID appears in this document and that no current requirement ID is duplicated across requirement files.
