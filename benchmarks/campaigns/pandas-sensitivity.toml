[campaign]
id = "pandas-sensitivity"
purpose = "Measure selected pandas-version sensitivity for HEAD with the fixed NumPy baseline."
revisions = ["HEAD"]
cases = ["pandas_lookup_numbers", "pandas_lookup_numbers_and_names", "pandas_lookup_inplace_numbers", "pandas_lookup_inplace_numbers_and_names"]
load_sizes = [10000, 100000, 1000000]
repetitions = 5
rounds = 3
environment_profile = "pandas-sensitivity"
record_samples = true
machine_policy = "same-machine-environment-case-version"
report_steps = []
