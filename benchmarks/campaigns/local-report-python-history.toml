[campaign]
id = "local-report-python-history"
purpose = "Populate two revisions across supported Python environments for native ASV report evaluation."

revisions = ["v0.4.0a6", "HEAD"]

cases = [
  "lookup_geographic_numbers",
  "lookup_seismic_numbers",
]

load_sizes = [10000, 100000, 1000000]

repetitions = 5
rounds = 3
environment_profile = "python-supported"
record_samples = true
machine_policy = "same-machine-environment-case-version"
report_steps = []
