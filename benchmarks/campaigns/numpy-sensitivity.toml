[campaign]
id = "numpy-sensitivity"
purpose = "Measure selected NumPy-version sensitivity for HEAD without changing pandas."
revisions = ["HEAD"]
cases = ["lookup_geographic_numbers", "lookup_seismic_numbers"]
load_sizes = [10000, 100000, 1000000]
repetitions = 5
rounds = 3
environment_profile = "numpy-sensitivity"
record_samples = true
machine_policy = "same-machine-environment-case-version"
report_steps = []
