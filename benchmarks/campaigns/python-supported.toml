[campaign]
id = "python-supported"
purpose = "Measure HEAD across the supported CPython versions with fixed NumPy and pandas versions."
revisions = ["HEAD"]
cases = ["lookup_geographic_numbers", "lookup_seismic_numbers"]
load_sizes = [10000, 100000, 1000000]
repetitions = 5
rounds = 3
environment_profile = "python-supported"
record_samples = true
